export enum imagesMessages {
  uploadSuccess = "Upload feito com sucesso",

  url = "URL do arquivo",

  imagePath = "Caminho do arquivo",
}

export enum ImagesRoutes {
  tags = "Imagens",
  baseUrl = "images",
}
