export enum ChecklistOnEmployeeFieldsProperties {
  tableName = "checklistOnEmployee",
  id = "id",
  employeeId = "employee_id",
  checklistId = "checklist_id",
  createdAt = "created_at",
  updatedAt = "updated_at",
}

export enum ChecklistOnEmployeeRoutes {
  base = "/checklistOnEmployee",

  tags = "ChecklistOnEmployee",
}
