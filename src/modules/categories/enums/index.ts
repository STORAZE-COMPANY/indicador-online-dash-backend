export enum CategoriesRoutes {
  baseUrl = "categories",
}

export enum CategoriesSwaggerInfo {
  tags = "Categories",
}

export enum CategoriesFieldsProperties {
  tableName = "categories",
  id = "Id da categoria de acesso",
  name = "Nome da categoria de acesso",
  created_at = "Data de criação",
  updated_at = "Data de atualização",
}
